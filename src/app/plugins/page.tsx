import Plugins from "@/components/plugins/plugins";

export default function PluginPage() {
  return (
    <main className="flex h-auto flex-col items-center justify-between overflow-hidden">
      <Plugins />
    </main>
  );
}